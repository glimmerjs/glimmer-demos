/* jshint node: true */

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'demo-d',
    environment: environment
  };

  return ENV;
};
/* jshint node: true */

module.exports = function(environment) {
  var ENV = {
    modulePrefix: 'glimmer-demos',
    environment: environment
  };

  return ENV;
};
